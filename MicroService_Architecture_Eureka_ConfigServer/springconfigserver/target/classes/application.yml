server:
  port: 7777

spring:
  application:
    name: springconfigserver

  cloud:
    config:
      server:
        git:
          uri: https://github.com/saumil-neekhra/lpu_MicroServices-config.git
          clone-on-start: true

management:
  endpoints:
    web:
      exposure:
        include: "*"